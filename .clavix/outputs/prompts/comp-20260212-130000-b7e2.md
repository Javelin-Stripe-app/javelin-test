---
id: comp-20260212-130000-b7e2
depthUsed: comprehensive
timestamp: 2026-02-12T13:00:00Z
executed: true
originalPrompt: "based on .ops/analysis/stripe-app-design-boundaries.md make the appropriate changes to claude.md, agents.md, product-vision-strategy, prd.md, config.yaml, .claude (remove skills that can't be used in this context)."
---

# Improved Prompt

Objective: Update all project configuration and governance files to enforce Stripe App
design boundaries documented in `.ops/analysis/stripe-app-design-boundaries.md`.

Source of Truth: `.ops/analysis/stripe-app-design-boundaries.md`

Files to Update (with specific changes per file):

1. **CLAUDE.md**
   - In "Stripe App UI" section: reinforce that no CSS framework (Tailwind, CSS Modules,
     styled-components) is supported — only `Box`/`Inline` with design token `css` prop
   - In "Architecture Boundaries" section: add explicit "Styling Constraints" subsection
     documenting the serialization model and why custom HTML/CSS is impossible
   - In "Coding Standards" section: add rule that all layout must use `Box` with design
     tokens — no raw HTML, no external CSS
   - In "Import guards" section: add guards for CSS imports and styling libraries

2. **openspec/config.yaml**
   - Add Stripe App design constraints as a validation rule or context section
   - Ensure artifact creation references the design boundaries analysis

3. **openspec/product-vision-strategy.md** (or relevant domain split)
   - Update tech stack / UI section to explicitly document the 34-component constraint
   - Add design boundary constraints (no Tailwind, no custom CSS, no DOM access)

4. **.claude/settings.json** or equivalent
   - Remove or disable skills that are inapplicable to Stripe Apps:
     - `web-design-guidelines` (references custom CSS, Tailwind, standard web patterns)
     - `frontend-design` (if it assumes custom HTML/CSS capability)
     - Any skill that recommends Tailwind, shadcn/ui, Headless UI, or custom CSS
   - Keep skills that remain valid: `interface-design` (if Stripe Toolkit-compatible),
     `ux-states`, `performance-patterns`, `security-patterns`, etc.

5. **prd.md** (if exists)
   - Add "Design Constraints" section referencing the Stripe App boundaries
   - Ensure UI requirements only reference Stripe UI Toolkit components
   - Remove or flag any references to custom CSS, Tailwind, or web-standard patterns

6. **agents.md** (if exists)
   - Update agent instructions to enforce Stripe UI Toolkit-only rule
   - Add pre-check: before any UI work, agent must verify component exists in Toolkit
   - Remove references to CSS frameworks or custom styling approaches

Approach:
- Read each target file first to understand current content
- Make minimal, targeted edits (no unrelated changes)
- Preserve existing structure and formatting
- Add constraints additively — don't remove valid non-UI rules

Validation Criteria:
- No file references Tailwind, CSS Modules, styled-components, or custom CSS as valid
- All UI guidance points to Stripe UI Toolkit components only
- Skills list excludes web-standard design skills incompatible with Stripe Apps
- `.ops/analysis/stripe-app-design-boundaries.md` is referenced as the authoritative source

## Quality Scores
- **Clarity**: 70%
- **Efficiency**: 75%
- **Structure**: 60%
- **Completeness**: 65%
- **Actionability**: 70%
- **Specificity**: 75%
- **Overall**: 69% (borderline → comprehensive depth applied)

## Original Prompt
```
based on .ops/analysis/stripe-app-design-boundaries.md make the appropriate changes to claude.md, agents.md, product-vision-strategy, prd.md, config.yaml, .claude (remove skills that can't be used in this context).
```

## Alternative Approaches

**1. Single-pass edit sweep**
Read all files, make all changes in one implementation pass.
Best for: Speed; all changes are independent.

**2. Analysis-first, then batch edit**
First read all target files and produce a change plan, then execute edits.
Best for: Reducing risk of unintended side effects; lets you review the plan.

**3. Incremental with verification**
Update one file at a time, verify each before moving to the next.
Best for: Safety; easier to catch mistakes early.

## Validation Checklist

- [ ] `CLAUDE.md` mentions no CSS frameworks as valid (Tailwind, CSS Modules, styled-components)
- [ ] `CLAUDE.md` has explicit styling constraints section referencing design tokens only
- [ ] `CLAUDE.md` import guards include CSS file imports and styling library imports
- [ ] `openspec/config.yaml` references Stripe App design boundaries
- [ ] `product-vision-strategy.md` documents the 34-component UI constraint
- [ ] PRD (if exists) has a Design Constraints section
- [ ] Inapplicable skills are removed from `.claude/` configuration
- [ ] No file suggests custom HTML elements are valid in extension views
- [ ] `.ops/analysis/stripe-app-design-boundaries.md` is referenced as source

## Edge Cases

- Skills that are partially valid (e.g., `interface-design` has useful layout advice but can't use custom CSS)
- Files that don't exist yet (`agents.md`, `prd.md` may not exist — skip or create?)
- Domain splits vs. full doc — changes should go in domain split `tech-architecture-baseline.md` per CLAUDE.md rules
- Skill removal scope — skills registered globally may affect other projects if shared
